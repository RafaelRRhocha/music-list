import type { FC } from 'react';
import ProfileEdit from '../../components/ProfileEdit';

interface editProps {}

const edit: FC<editProps> = ({}) => {
  return (
    <ProfileEdit />
  );
}

export default edit;